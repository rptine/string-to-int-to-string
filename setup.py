from setuptools import setup

setup(name='string_to_int_to_string',
      version='0.1',
      description='Convert strings to integers, and back to strings in a deterministic fashion',
      url='https://github.com/rptine/string_to_int_to_string.git',
      author='Ryan Tine',
      author_email='ryanptine@gmail.com',
      license='MIT',
      packages=['string_to_int_to_string'],
      zip_safe=False)